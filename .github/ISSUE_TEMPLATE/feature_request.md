---
name: Feature request
about: A tmux subcommand or arg shape the shim should handle
labels: enhancement
---

## What does Claude call?

<!--
The exact `tmux ...` invocation Claude makes that the shim doesn't handle today.
If you can, capture it from $NVIM_TMUX_LOG and paste it here.
-->

```
# example: tmux split-window -h -t %42 -P -F '#{pane_id}'
```

## Why does Claude need it?

<!-- Which Agent Teams flow surfaces this? What breaks today? -->

## Proposed translation

<!-- How should the shim map this to nvim? Optional but appreciated. -->
